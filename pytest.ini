[pytest]
testpaths = tests
pythonpath = .
addopts = -ra --html=report.html --self-contained-html --junitxml=report.xml
markers =
    smoke: fast subset of critical-path checks
    regression: full functional coverage
